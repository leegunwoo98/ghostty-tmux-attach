# ghostty-tmux-attach

Makes Ghostty's `window-save-state` restore actually re-attach to tmux sessions
instead of dropping users at a plain `$HOME` shell.

**Status:** v0.1 in development. README polish pending Phase 11.
